"use client";

import styled from "styled-components";

export const MaxWidthContainer = styled.div`
  margin: 0 auto;
  max-width: var(--breakpoint-tablet-landscape);
  flex-wrap: wrap;
`;

export const MaxWidthContainerSmall = styled.div`
  margin: 0 auto;
  max-width: var(--breakpoint-tablet);
  flex-wrap: wrap;
`;
