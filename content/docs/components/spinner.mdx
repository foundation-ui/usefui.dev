---
title: Spinner
description: Spinners are used to convey a pending state.
---

<DocsSection>

<DocsPreview language="tsx" component={<SpinnerTemplate />} code={`import React from "react";
import { Spinner } from "@usefui/components";

function SpinnerTemplate() {

    return (
      <div className="grid g-medium-30 p-t-large-30 p-x-medium-60">
        <div className="flex align-center g-medium-10 justify-center">
          {["small", "medium", "large"].map((size, key) => (
            <Spinner key={key} sizing={size as "small" | "medium" | "large"} />
          ))}
        </div>
      </div>
    );

}

export default SpinnerTemplate;
`} />

</DocsSection>

<DocsSection>

### Anatomy

<DocsTextBlock className="m-b-medium-60">
  The Spinner component **_is not_** built using the [Compound
  pattern](https://www.patterns.dev/react/compound-pattern/), it carries all the
  properties required to make to component work as intended.
</DocsTextBlock>

<DocsCodePreview
  language="tsx"
  code={`import { Spinner } from "@usefui/components";

export function SpinnerComponent() {
    return <Spinner />;
}
`} />

</DocsSection>

### API Reference

<DocsSection className="m-y-large-10">

<DocsPropsTable
  properties={[
    {
      name: "sizing",
      type: '"small" | "medium" | "large"',
      defaultValue: "medium",
      required: false,
    }
  ]}
/>
</DocsSection>

<DocsFooter prev="/docs/components/skeleton" next="/docs/components/switch" />
