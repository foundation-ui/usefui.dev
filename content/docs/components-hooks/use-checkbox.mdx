---
title: useCheckbox
description: Import and use the Checkbox component's Hooks in your project.
---

<DocsCodePreview
  language="tsx"
  code={`import { useCheckbox } from "@usefui/components";

export function HookComponent() {
    const checkbox = useCheckbox();
    const { id, states, methods } = checkbox;

    const { checked } = states;
    const { toggleChecked, applyChecked } = methods;

    return ( {/** Your code here **/} );

}
`} />

<DocsSection className="m-y-large-10">
### API Reference

<DocsTextBlock>
  <p>The properties and functions returned by the useCheckbox hook.</p>
</DocsTextBlock>

</DocsSection>

<DocsPropsTable
  properties={[
    {
      name: "id",
      type: "string",
      defaultValue: "React.useId",
    },
    {
      name: "checked",
      type: "boolean",
      defaultValue: "false",
    },
    {
      name: "toggleChecked",
      type: "(): => void",
    },
    {
      name: "applyChecked",
      type: "(state: boolean) => void",
    },
  ]}
/>

<DocsFooter
  prev="/docs/components-hooks/use-accordion"
  next="/docs/components-hooks/use-collapsible"
/>
