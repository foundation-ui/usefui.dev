---
title: useToolbar
description: Import and use the Toolbar component's Hooks in your project.
---

<DocsCodePreview
  language="tsx"
  code={`import { useToolbar } from "@usefui/components";

export function HookComponent() {
    const toolbar = useToolbar();
    const { id, states, methods } = toolbar;

    const { expanded } = states;
    const { toggleToolbar } = methods;

    return ( {/** Your code here **/} );

}
`} />

<DocsSection className="m-y-large-10">
### API Reference

<DocsTextBlock>
  <p>The properties and functions returned by the useToolbar hook.</p>
</DocsTextBlock>

</DocsSection>

<DocsPropsTable
  properties={[
    {
      name: "id",
      type: "string",
      defaultValue: "React.useId",
    },
    {
      name: "expanded",
      type: "boolean",
      defaultValue: "false",
    },
    {
      name: "toggleToolbar",
      type: "(state: boolean): => void",
    },
  ]}
/>

<DocsFooter
  prev="/docs/components-hooks/use-tabs"
  next="/docs/icons/installation"
/>
